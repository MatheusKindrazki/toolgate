# AGENTS.md

Run `make check` before committing. Never add a privileged helper, Full Disk Access requirement, Endpoint Security, or Network Extension. Treat adapter input as untrusted and redact before persistence. Only claim `enforced` for a documented synchronous adapter contract; keep Codex and Hermes `unsupported` until independently verified.
